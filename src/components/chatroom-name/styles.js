import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  chatroomNameContainer: {
    paddingVertical: 4,
    marginBottom: 4,
    borderRadius: 8,
  },

  chatroomNameText: {
    textAlign: "center",
  },

  chatroomNameTextHighlight: {
    fontWeight: "bold",
  },
});

export default styles;
